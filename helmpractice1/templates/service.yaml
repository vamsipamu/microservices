apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmpractice1.fullname" . }}
  labels:
    {{- include "helmpractice1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmpractice1.selectorLabels" . | nindent 4 }}
