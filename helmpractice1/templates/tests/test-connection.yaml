apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmpractice1.fullname" . }}-test-connection"
  labels:
    {{- include "helmpractice1.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmpractice1.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
